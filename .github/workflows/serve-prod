# This is a basic workflow to help you get started with Actions

name: CI - prod ng serve

on:
  push:
    branches: [ "prod" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Serve Angular app
        run: npm run start
